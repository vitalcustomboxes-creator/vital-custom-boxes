# ISSUES LOG (append; tick when fixed; severity: S1 blocker / S2 major / S3 minor)
- [x] [PM-DECISION] APPROVED: add `/locations/` → `/custom-pizza-boxes/` redirect (count becomes 264). Owner: BE-2 (append via scripts/gen-redirects.mjs EXTRA list or manual entry + update docs/seo/REDIRECTS.md). (S3) **DONE by BE-2 2026-06-12: EXTRA group 5 added to scripts/gen-redirects.mjs (asserts updated 263→264), lib/redirects.ts regenerated (grep-verified 264 entries / 222 `/locations/` sources), next.config.ts count comments updated, REDIRECTS.md §1/§5/§6 updated.**
- [x] [PM-DECISION] HTML sitemap keeps live URL `/sitemap/`: BE-2 creates app/sitemap/page.tsx (coexists with app/sitemap.ts → /sitemap.xml). Do NOT use /sitemap-page. (S3) **DONE by BE-2 2026-06-12: app/sitemap/page.tsx serves the HTML sitemap at /sitemap/ (pages + 22 categories + 153 products + 16 posts from loaders; excludes /thank-you + /case-studies); build verified — /sitemap (page) and /sitemap.xml (metadata route) coexist. Meta strings reuse STATIC_PAGE_META["/sitemap-page"] with path "/sitemap/".**
- [x] [SEO-1] next.config.ts lacked `trailingSlash: true` — without it none of the 263 trailing-slash redirect sources can match, and every live URL form would 308-hop at launch. Fixed by SEO-1 directly in next.config.ts (commented in-file); ARCHITECT please preserve. If policy ever changes, flip TRAILING_SLASH in scripts/gen-redirects.mjs and regenerate lib/redirects.ts. (S2)
- [x] [SEO-1] Live `/locations/` hub page is linked in the live header nav but has NO redirect in the 263-entry spec and no new route → will 404. Recommend PM approve adding `/locations/` → `/custom-pizza-boxes/` (config count becomes 264). (S3) **DONE — see PM-DECISION tick above (BE-2).**
- [x] [SEO-1] Live HTML sitemap page is `/sitemap/`; new IA names the route `/sitemap-page` with no redirect specced → `/sitemap/` will 404. BE-2: add redirect `/sitemap/` → `/sitemap-page/` or rename the route to `/sitemap`. (S3) **DONE — PM chose the route-name option: HTML sitemap lives AT /sitemap/ (no redirect needed, zero-hop live-URL parity). /sitemap-page does not exist (BE-2).**
- [x] [SEO-1] `/thank-you` must be `noindex` AND excluded from sitemap.ts — the live WP sitemap wrongly includes it; do not copy. Spec in docs/seo/TECH_SEO.md §3–4. For BE-2. (S3) **DONE by BE-2 2026-06-12: buildMetadata noIndex:true (rendered `<meta name="robots" content="noindex, nofollow">` verified in build output), excluded from app/sitemap.ts (211 `<loc>` entries, zero `thank-you` — grep-verified on built sitemap.xml), and `Disallow: /thank-you/` in app/robots.ts.**
- [x] [SEO-2] Live copy contradictions must NOT be migrated: "no minimum", "Free worldwide shipping over $100", turnaround variants (2-3 weeks / 7-15 days / 4-8 days / 3-7 days), mis-grouped phone "+1 (213) 6926-437", broken tel:+1-078-2376 — all SLA/MOQ/shipping/phone copy comes from content/globals.json. Banned-claims list: docs/seo/CONTENT_GUIDELINES.md §8. (S2 — DATA-ENG/FE-3/BE-1) **[DATA-ENG portion DONE 2026-06-12: all content JSON normalized to globals; §8 banned-claims scan enforced in scripts/validate-content.mjs → PASS. Remaining: FE-3/BE-1 render claims only via getGlobals().]** **[BE-1 portion DONE 2026-06-12 (FE-3 done per row 37 → fully closed): layout/home/[category]/hub/product render every SLA/MOQ/shipping/phone/promo string from getGlobals() via props (TrustBar, ProcessSteps, SpecTable `globals` prop, CTABand tel, hero lead composed from globals.sla; StatsRow figures = content counts or regex-parsed FROM globals at render — drop on parse failure, never hardcoded). Verified in prerendered HTML.]**
- [x] [SEO-2] mylar-bags / custom-cbd-boxes / custom-tobacco-packaging need compliance wording + required disclaimer line per CONTENT_GUIDELINES §7 (no concealment language, "designed to help meet" for child-resistant, no health claims). (S2 — DATA-ENG/BE-1) **[DATA-ENG portion DONE 2026-06-12: §7 wording applied to category/product copy; categories flagged `regulated: true`; disclaimer shipped as globals.complianceDisclaimer. Remaining: BE-1 renders the disclaimer on those 3 category pages + their product pages.]** **[BE-1 DONE 2026-06-12: `category.regulated` drives a role="note" banner with globals.complianceDisclaimer in the T2 hero (all 3 categories) and in the T3 info column for all 16 products in those categories. Verified in built HTML (mylar-bags, custom-cigarette-boxes present; bakery absent).]**
- [x] [SEO-2] trailingSlash coordination: live URLs use trailing slashes; lib/seo.ts canonicals normalize to NO trailing slash (Next default). ARCHITECT: keep trailingSlash=false or tell SEO-2 to update toAbsoluteUrl; SEO-1: redirect targets must match the same convention. (S2 — ARCHITECT/SEO-1) **RESOLVED by SEO-2 same-day: SEO-1 wired `trailingSlash: true` with live evidence (TECH_SEO §1); `toAbsoluteUrl` now emits trailing-slash canonicals (lowercase, query/fragment stripped); both SEO docs aligned.**
- [x] [SEO-2] DEFAULT_OG_IMAGE + ORG_LOGO_URL in lib/seo.ts point at live wp-content asset URLs (per brief). At cutover those WP paths die — self-host logo/default OG and update the two constants. (S3 — ARCHITECT/DEVOPS) **FIXED by DEVOPS 2026-06-12: self-hosted public/og-default.svg (1200×630 hand-built branded card: ink-900 bg, terra accent bar, wordmark) + public/logo.svg; lib/seo.ts constants now `${SITE_URL}/og-default.svg` / `${SITE_URL}/logo.svg` (two-constant edit to SEO-2's file, comment updated; stale eslint-disable in devWarn also removed). Follow-ups logged at end of this file: logo is a recreation (live SVG unfetchable from sandbox) and og:image should become PNG before launch.**
- [x] [SEO-2] /case-studies and /portfolio render the same content — set canonical of /case-studies → /portfolio (live page with equity) or differentiate copy. (S3 — BE-2) **DONE by BE-2 2026-06-12: /case-studies/ emits `<link rel="canonical" href="…/portfolio/">` (verified in built HTML) via buildMetadata path:"/portfolio/", shares CaseStudyShowcase content with /portfolio/, and is excluded from sitemap.ts per TECH_SEO §2.5/§3.**
- [ ] [SEO-2] lib/seo.ts is SERVER-ONLY (node:fs in getSiteGlobals) — never import from "use client" components; render <JsonLd> in server pages/layouts. (S3 — FE-2/FE-3/BE-*)
- [x] [DATA-ENG] /business-card category page is missing from page-sitemap.xml (it exists — linked in live nav as "Custom Business Cards"), so no hero image was available; categories.json uses its product hero (2025/05/Custom-Business-Card-Boxes-4.jpg). DESIGNER/FE may pick a better hero; SEO-VERIFY: confirm /business-card IS included in OUR generated sitemap.ts. (S3) **SEO-VERIFY portion DONE 2026-06-12: built sitemap.xml (.next/server/app/sitemap.xml.body) contains exactly 1 `<loc>https://www.hmcustompackaging.com/business-card/</loc>` among 211 total entries; /business-card/ also live-tested 200 and its 31 city doorways 308 to it (evidence: docs/seo/SEO_VERIFICATION.md §1–2). Hero-image choice stays optional with DESIGNER/FE.**
- [ ] [DATA-ENG] lib/content.ts is SERVER-ONLY (node:fs) like lib/seo.ts — same rule applies: import only from server components/route handlers. ARCHITECT: optionally add the `server-only` package guard (comment slot ready at top of lib/content.ts). (S3 — FE-*/BE-*/ARCHITECT)
- [x] [DESIGNER] Root layout MUST set `data-js` on <html> — first thing in <head>: `<script dangerouslySetInnerHTML={{__html:"document.documentElement.setAttribute('data-js','')"}} />`. styles/animations.css scopes all scroll-reveal hidden states under html[data-js] so content is never invisible without JS; missing script = no scroll animations site-wide. Exact snippet in docs/DESIGN_SPEC.md §0.3. (S2 — BE-1) **DONE by BE-1 2026-06-12: exact snippet rendered as first child of an explicit <head> in app/layout.tsx (+ suppressHydrationWarning on <html> for the pre-hydration attribute). React 19 hoists charset/viewport/CSS ahead of layout head children — position-0 is impossible in App Router, but the script still executes during head parse, i.e. before any body content can paint, which is what the reveal CSS needs. Verified in prerendered HTML: one <head>, script before all JS chunk tags.**
- [ ] [DESIGNER] Fixed-palette contrast constraints (cannot change hexes — enforce usage rules): white on terra-500 = 3.2:1 → WCAG AA large-text only, so CTA button labels ≥16px font-semibold and NEVER small white text on terra-500; terra-600 on paper-50 ≈ 4.3:1 → accent text ≥14px semibold only; slate-400 on white ≈ 3.1:1 → decoration/placeholders/disabled ONLY, never essential text (body/meta = slate-600). Rules table in docs/DESIGN_SPEC.md §1.1 — FE-* implement, QA-MANUAL verify. (S3)
- [x] [ARCHITECT] `npm run lint` not yet runnable — eslint 9 installed but no eslint config exists (flat-config era; DEVOPS owns). Add eslint.config.mjs (or .eslintrc) extending next/core-web-vitals + next/typescript. (S3 — DEVOPS) **FIXED by DEVOPS 2026-06-12: eslint.config.mjs (flat config; FlatCompat bridges next/core-web-vitals + next/typescript; global ignores .next/node_modules/out/coverage/public/data). package.json: lint script `next lint`→`eslint .` (next lint deprecated in Next 15.5, removed in 16) and @eslint/eslintrc added to devDependencies via npm install — lockfile synced, `npm ci` safe. Verified: `npm run lint` = 0 errors 0 warnings on current tree.**
- [x] [ARCHITECT] npm resolved typescript@6.0.3 which conflicts with Next 15.5/typescript-eslint peer ranges — pinned to typescript@^5.9.3 and @types/node@^22 in package.json. Do not bump TS major without checking peers. (S3, fixed by ARCHITECT)
- [ ] [BE-3] FE-3 QuoteForm hardcodes its own option labels + field names (`depth`, `colors`, "12pt Cardstock", "Soft Touch", lamination "None"…) which drift from the live-form canon in lib/forms.ts. Server BRIDGES all known variants via aliases in `quoteFromFormData` (test-pinned), so submissions work today — but FE-3 should import `QUOTE_OPTIONS`/`HONEYPOT_FIELD` from `@/lib/forms` (client-safe) and ideally rename depth→height, colors→color to kill the drift. (S3) **[QA-AUTO 2026-06-12 caution: importing from @/lib/forms in the client would RE-ADD zod to the quote bundle (see budget fix below) — do this only together with a constants-only module split (zod-free) of lib/forms.]**
- [x] [BE-3] FE-3 QuoteForm client validation allows quantity ≥1 but the server enforces MOQ 25–100,000 — users entering <25 only hit the error after submit. FE-3: set `min={25}` + client-side check (help text already shows globals.moq). (S3) **DONE by QA-AUTO 2026-06-12 (in the same QuoteForm budget edit): `MIN_QUANTITY = 25` validator (message "whole number of 25 or more") + `min={25}` on the input; comment in-file points at lib/forms quoteSchema as the source of the floor.**
- [ ] [BE-3] With `trailingSlash: true`, the route handler answers at `/api/search/` and `/api/search?q=` 308-redirects (verified on dev server). FE-2/any fetch consumer: call `/api/search/?q=` directly to skip the hop. (S3 — informational)
- [ ] [BE-3] Duplicate search implementations: lib/content.ts `search()` (DATA-ENG, simpler ranking) vs lib/search.ts (BE-3, canonical for API/UI: startsWith > includes > description). Consumers must use lib/search.ts; suggest DATA-ENG deprecate/remove theirs in a later wave to avoid confusion. (S3)
- [x] [DEVOPS] `npm run build` FAILS right now: app/page.tsx + app/products/[slug]/page.tsx import `@/components/patterns/TrustBar` and `@/components/patterns/RelatedProducts`, but the files live in `components/blocks/` — BE-1/FE-3 agree on one path. Until green, the CI build/check-budgets/smoke stages stay red (smoke verified mechanically: 7/7 routes 500 in dev fallback, table + teardown OK). (S1 mid-wave — BE-1/FE-3) **RESOLVED by BE-1 2026-06-12: imports point at `components/blocks/TrustBar` + `components/blocks/RelatedProducts` (agreed path = blocks/). `npx tsc --noEmit` = 0 errors; `next build` PASS — / + 22 categories + 153 products prerendered, routes 118–142 kB first-load.** **[BE-2 verification 2026-06-12, end of wave: condition no longer reproduces — full `npx next build` PASSES (✓ compiled, 219/219 static pages, all routes in table, first-load 103–143 kB ≤ budget). Leaving the tick to DEVOPS for a CI re-run.]**
- [x] [DEVOPS] vitest currently RED: tests/search.test.ts 2/8 failing against lib/search.ts ("returns typed hits with trailing-slash hrefs per content type", "finds mylar content and puts the starts-with match first") — looks mid-flight (lib/forms.ts was being written during the run). Not a config issue: the dual-project vitest setup executes them in the same node env/aliases as BE-3's original config. (S2 — BE-3) **RESOLVED same-day: BE-3 landed lib/search.ts + shim fix while this was being logged; DEVOPS re-ran `npx vitest run` → 4 files / 35 tests ALL PASS (node + dom projects).**
- [ ] [DEVOPS] public/logo.svg is a token-faithful RECREATION, not the live file — the real logo3.svg could not be copied from this sandbox (web_fetch strips image/svg+xml to empty; shell egress is allowlist-blocked except npm/github; wayback blocklisted; no browser bridge connected). At cutover: download https://www.hmcustompackaging.com/wp-content/themes/hm-packaging/assets/images/icon/logo3.svg in any browser and replace public/logo.svg 1:1 — the path is stable, no code changes (swap note also embedded in the SVG itself). (S3 — DEVOPS/human)
- [ ] [DEVOPS] og:image is an SVG (/og-default.svg) — several social scrapers (Facebook/LinkedIn/X) ignore SVG og:image. Before launch: export the card as 1200×630 PNG (public/og-default.png) and flip DEFAULT_OG_IMAGE in lib/seo.ts. (S3 — DEVOPS/DESIGNER)
- [ ] [DEVOPS] Deploy/launch gaps (per-brief stubs, now documented in README §Operations): data/leads.jsonl is EPHEMERAL on Vercel serverless (function logs are the only durable lead copy until then) — wire an email provider (Resend/Postmark/SES); no analytics installed; Turnstile bot-protection is stubbed (needs real keys). (S3 — PM/BE-3 at cutover)
- [x] [FE-1] PascalCase deep imports of components/ui files (ui/Breadcrumbs, ui/Button…) hit TS1149 casing clashes on the case-insensitive host FS — ui files are kebab-case. Fixed by FE-1 in PageHero.tsx + app/contact/page.tsx (both BE-2, commented in-file). CONVENTION: import from the barrel `@/components/ui`; deep imports must be lowercase. (S2, fixed)
- [ ] [FE-1] lucide-react 1.18 removed the legacy aliases DESIGN_SPEC §5 mentions: use LoaderCircle (not Loader2), CircleAlert (not AlertCircle), CloudUpload (not UploadCloud), CircleCheck (not CheckCircle2). ui/* already complies — FE-2/FE-3/BE-* check your imports. (S3 — informational)
- [ ] [FE-1] animations.css has no `.drawer-panel--bottom`; ui/Drawer side="bottom" replicates the slide with transform utilities (duration token, neutralised by the global reduced-motion kill switch). DESIGNER may add an official variant for parity. (S3 — DESIGNER, optional)
- [x] [FE-1] `<ToastProvider>` (components/ui/toast.tsx) is not wired yet — BE-1: wrap the root layout body content once so useToast() works (QuoteForm server-failure toast per spec §6.9). (S2 — BE-1) **DONE by BE-1 2026-06-12: ToastProvider wraps the full body content in app/layout.tsx (children stay server-rendered); any client component can useToast(). NB row 36: QuoteForm currently uses its aria-live summary instead — provider is ready either way.**
- [x] [FE-1] typecheck red OUTSIDE ui scope (FE-1 scope verified clean, smoke 4/4): tests/actions.test.ts expects `{ok,error}` from void-typed actions (5 errors — BE-3); BE-1 app pages pass props the patterns don't declare (~20 errors: Hero `title`, ProductCard `categoryName`, FAQAccordion `faqs` vs `items`, CTABand `heading`, PromoBar `promo` vs `globals`, SpecTable missing `globals`, ReviewWall `trustpilotUrl`, app/layout MobileNavDrawer path, Breadcrumb crumbs now fine via FE-1 `name` alias). Overlaps DEVOPS' S1 build issue. (S2 — BE-1/FE-2/FE-3/BE-3) **CLOSED 2026-06-12: the listed BE-1 prop mismatches were mid-flight contract drift (both sides building in parallel); BE-1 reconciled all five pages against the final component APIs. BE-3 test errors resolved per rows 25/34/38. Repo-wide `npx tsc --noEmit` = 0 errors + `next build` PASS, re-verified by BE-1 after wiring ToastProvider.**
- [x] [BE-3] types/fe-contracts.d.ts (BE-2 shim) — the "ambient declarations yield once the real file lands" assumption is WRONG: `declare module "@/..."` SHADOWS path-mapped resolution even when the real file exists. It was overriding app/actions.ts types repo-wide (actions typed Promise<void>, broke BE-3 test typecheck). BE-3 removed its own block (sanctioned by the shim header; noted in-file + report). The FE component blocks STILL shadow the now-real components (e.g. real QuoteForm props `categories:{slug,name}[]`/`moq` vs shim `categories:Category[]`/`globals`) and will mask prop-contract mismatches in BE-2 pages until deleted — BE-2/QA-AUTO: reconcile pages against real components and delete the shim. (S2) **RESOLVED by BE-2 2026-06-12: shim file DELETED entirely (types/ dir removed) and every BE-2 page reconciled against the real component APIs (QuoteForm action/categories/moq/defaultProduct, CTABand heading, FAQAccordion faqs + section-owning, ReviewWall wrapper, InteriorHero breadcrumbs slot, ui barrel imports). Verified: repo-wide `npx tsc --noEmit` exit 0 + full `next build` 219/219 pages. Thanks BE-3/FE-2 for the shadowing find.**
- [x] [FE-3] app/faqs/page.tsx wrapped the (section-owning) FAQAccordion in an extra `.section` wrapper → double vertical padding; unwrapped in place (one-line fix in BE-2's file, see reports/FE-3.md §4). (S3)
- [x] [FE-3] QuoteForm surfaces server-action failures in its aria-live error summary instead of an error Toast (DESIGN_SPEC §6.9 says Toast) — intentional: one focus-managed live region, no Toast dependency in the form. QA-MANUAL: confirm acceptable or FE-3 will wire ui/toast. (S3) **ACCEPTED by QA-MANUAL 2026-06-12: single assertive live region with focus management is equal-or-better than a Toast for SR users and keeps the route bundle lean (QA-AUTO's zod removal compounds this); verified rendered markup (`role="alert" aria-live="assertive"` summary). ToastProvider stays wired in the layout for future consumers. No change requested — see docs/qa/MANUAL_QA.md F-04.**
- [x] [FE-3] SEO-2 banned-claims item (#8), FE-3 portion DONE: every FE-3 component takes SLA/MOQ/shipping/phone via props only (fed from getGlobals() by pages); ReviewWall/StatsRow ship zero default numbers, Trustpilot/Verified badges gated on verified===true. Remaining: BE render-check stays with BE-1. (S2→note)
- [x] [FE-2] FE-2 portion of the typecheck/shim items above is RESOLVED: removed the stale FE-2 CTABand block from types/fe-contracts.d.ts (it SHADOWED the real CTABandProps — same finding as BE-3); CTABand `heading` now optional with a claim-free default; PromoBar accepts `{globals}` OR `{promo}`; BE-1 reconciled layout/home live. Repo-wide `npx tsc --noEmit` re-verified by FE-2 = **0 errors**; `eslint` on all 11 FE-2 files = 0/0. (S2 → resolved 2026-06-12)
- [ ] [FE-2] `globals.address` still contains "Los Angeles, CA (TODO client: street address)" — Footer strips the `(TODO …)` parenthetical at render so it never ships visibly, but the client MUST supply the real street address in content/globals.json before launch (also feeds LocalBusiness schema). (S3 — client/PM)
- [ ] [FE-2] lucide-react v1 ships NO brand icons (Facebook/Instagram/Linkedin/Twitter/Pinterest removed) — Footer renders minimal inline-SVG glyphs (aria-hidden) inside labeled 36px round links. If brand icons are needed elsewhere, reuse Footer's `SocialGlyph` helper rather than adding a dependency. (S3 — informational, handled)
- [ ] [FE-2] Header IA follows the PM brief and intentionally differs from DESIGN_SPEC §6.1's example nav: actual nav = Home · By Industry ▾ · By Material ▾ · By Style ▾ · Portfolio · Contact + search popover (GET /products/?q=, visible label) + phone ghost + Get Free Quote; Locations REMOVED (old URLs covered by SEO-1 redirects); mega panels = category image tiles grouped by `navGroup` + "View all" → /industries/ /materials/ /box-styles/. DESIGNER/QA FYI — do not QA literally against the §6.1 example list. (S3 — informational)
- [x] [FE-2] StickyMobileCTA hides over `[data-quote-form], #quote-form, footer` (IO) and never renders on /get-custom-quote. FE-3: add `data-quote-form` to the QuoteForm card root so the bar also yields to quote forms EMBEDDED on other pages; any page section may opt in with the same attribute. (S3 — FE-3) **DONE by QA-AUTO 2026-06-12: `data-quote-form=""` added on the QuoteForm `<form>` root (same edit as the budget fix, commented in-file).**
- [ ] [FE-2] FE-1 Drawer's content area hardcodes `p-5` and `cx` is a plain join (no tailwind-merge), so MobileNavDrawer overrides with `!p-0` for full-bleed rows. FE-1: consider `padded?: boolean` or a replacing (not appending) contentClassName to avoid `!important`. (S3 — FE-1, polish)
- [ ] [BE-2] /how-it-works needs SIX steps (PM brief) but FE-3's ProcessSteps is fixed at the canonical 4 with no `steps` prop — the page renders its own 6-step rail with the identical §6.12 recipe (Reveal stagger, number chips, "Step N" overlines). If another 6-step consumer appears, FE-3 should add an optional `steps` override and the page will switch to it. (S3 — informational/FE-3 optional)
- [ ] [BE-2] /get-custom-quote/ renders dynamically (ƒ in the build table) because it awaits `searchParams` to resolve `?product=<slug>` into QuoteForm's `defaultProduct` (FE-3 contract). Intentional — it is a form page, not a content page; do not "fix" it back to static without removing the preselect feature. (S3 — informational)
- [ ] [BE-2] /contact/ shows phone hours "Monday–Friday, 9 am–6 pm Pacific" with a TODO comment — client must confirm real hours before launch (same launch-blocker class as the street address, FE-2 item above). (S3 — client/PM)
- [ ] [BE-2] Legal pages (/terms-conditions /shipping-policy /return-policy /privacy-policy) are SHORT consistency rewrites sourced from globals (SLA/MOQ/shipping) with `TODO legal review` comments in-file: counsel must sign off on the 7-business-day defect window, liability cap, and privacy jurisdiction wording before launch. Terms deliberately state food-safe material options are AVAILABLE ON REQUEST — the live food-grade contradiction is gone; keep future edits consistent with product claims. (S3 — client/PM)
- [ ] [BE-2] Blog post bodies are still DATA-ENG's flagged "TODO-migrate" placeholders. /blog/[slug] already supports `## ` heading lines (anchored h2s + TOC) — when the 16 live articles are migrated verbatim into posts.json, headings/TOC light up with no code change. AuthorBox stays the team byline (no invented personas). (S3 — content migration/client)
- [ ] [BE-2] Cosmetic: the HTML sitemap route is /sitemap/ (PM decision) but its meta strings live under the `"/sitemap-page"` key in lib/seo.ts STATIC_PAGE_META — SEO-2 may rename the key to `"/sitemap"` for clarity (lookup is by literal key; no behavior impact). (S3 — SEO-2, optional)
- [ ] [BE-1] next/font/google CANNOT run in this sandbox (fonts.googleapis.com blocked; egress is npm-only — curl verified) and would hard-fail `next build`, so app/layout.tsx ships WITHOUT next/font; tokens.css fallback stacks (var(--font-poppins, 'Poppins', …)) apply. AT CUTOVER restore the DESIGN_SPEC §0.2 snippet — preserved verbatim in the app/layout.tsx header comment (same --font-poppins/--font-manrope variable names). (S3 — BE-1/DEVOPS at cutover)
- [ ] [BE-1] Root-layout default metadata (per task brief: buildMetadata defaults) includes a canonical for "/" — Next metadata inheritance means any route that does NOT export its own metadata silently inherits that canonical. All BE-1 routes override; BE-2/SEO-VERIFY: confirm every other route exports per-page buildMetadata (grep for routes missing `export const metadata`/`generateMetadata`). (S3 — BE-2/SEO-VERIFY)
- [ ] [BE-1] /products is request-rendered (ƒ dynamic) because it reads searchParams for the no-JS `?q=` filter (visible-label GET form; results ranked via BE-3 lib/search on a products-only corpus). Expected + accepted; all 153 product and 22 category routes remain fully static. FYI DEVOPS (ISR/cache headers if ever needed) + QA. (S3 — informational)
- [ ] [SECURITY→DEVOPS] No Content-Security-Policy header (deliberately deferred to SECURITY per next.config.ts comment). ADD at deploy in next.config.ts headers() (or vercel.json). Draft policy in docs/qa/SECURITY_REPORT.md §5: `default-src 'self'; base-uri 'self'; object-src 'none'; frame-ancestors 'none'; form-action 'self'; img-src 'self' https://www.hmcustompackaging.com data:; style-src 'self' 'unsafe-inline'; script-src 'self' 'unsafe-inline'; connect-src 'self'; font-src 'self'; upgrade-insecure-requests;`. `'unsafe-inline'` needed for the data-js snippet + inline JSON-LD + Next/Tailwind inline styles; ship Report-Only first, then enforce; nonce-based CSP later to drop script-src 'unsafe-inline'. (S2)
- [ ] [SECURITY→DEVOPS] Lead storage (data/leads.jsonl) + in-memory rate limiter are EPHEMERAL on serverless — leads lost on cold start, rate-limit is per-instance + XFF-spoofable. At deploy: wire real email provider (Resend/Postmark/SES, key via env) + move rateLimit() to a shared store (Upstash/Vercel KV) + wire real Turnstile (verify token server-side in actions). If the `artwork` upload is ever enabled, enforce MIME+magic-byte+size SERVER-SIDE (FE accept= is bypassable). Define PII retention once leads are stored durably. (S2)
- [x] [SECURITY→QA-AUTO/DEVOPS] `poweredByHeader` not set in next.config.ts → Next default `true` → `X-Powered-By: Next.js` header is emitted (framework fingerprinting). Set `poweredByHeader: false`. (S3) **DONE by QA-AUTO 2026-06-12: `poweredByHeader: false` added to next.config.ts (commented, attributed); full gate suite re-run green after the change (tsc/lint/vitest/build/budgets/smoke/qa-crawl) and header absence verified on a live `next start` response.**
- [ ] [SECURITY→DEVOPS] `npm audit` = 2 moderate, both from postcss <8.5.10 BUNDLED inside next@15.5.19 (node_modules/next/node_modules/postcss@8.4.31); project's own postcss devDep is ^8.5.15 (patched). Transitive, not directly fixable; only npm remediation is a next→9.3.3 downgrade (do NOT run `npm audit fix --force`). Non-exploitable here (no untrusted CSS stringified). Re-check after each next bump — clears when next bundles postcss ≥8.5.10. (S3)
- [ ] [SECURITY→DEVOPS] HSTS is `max-age=63072000` with NO includeSubDomains/preload (intentional pre-cutover). At cutover once subdomain inventory is known: add includeSubDomains (+ preload + submit to preload list); confirm host redirects HTTP→HTTPS. (S3)
- [x] [QA-AUTO] First-load budget gate FAILED mid-wave: /get-custom-quote = 151.3 kB gzip (>150; `next build` column showed 143 — DEVOPS' stricter per-file gzip accounting in scripts/check-budgets.mjs crossed the line). Root cause: FE-3 QuoteForm ('use client') imported zod for UX-only validation → ~18.5 kB gzip chunk on the route. FIXED by QA-AUTO in components/patterns/QuoteForm.tsx: zod schemas → dependency-free per-field validators (messages/semantics 1:1; server still zod-validates via BE-3); route now 133.0 kB. Budgets 26/26 PASS. (S2 → fixed 2026-06-12)
- [ ] [QA-AUTO→BE-3/FE-3] lib/forms.ts is imported by the client LeadForm (only for HONEYPOT_FIELD + a type), which keeps zod's chunk in the /contact and /samples client bundles — they pass at 147.3 kB but are the heaviest routes. Suggest splitting a zod-free `lib/forms-constants.ts` (HONEYPOT_FIELD, QUOTE_* option lists, ActionResult type) for client imports; that same split is the prerequisite for the ISSUE-#20 option-list unification. (S3)
- [ ] [QA-AUTO→DEVOPS] FYI: appended a `QA crawl` stage to .github/workflows/ci.yml after smoke (node scripts/qa-crawl.mjs — 24-page sample: single h1, ≤1 FAQPage JSON-LD, BreadcrumbList on inner pages, undefined/NaN/[object Object] literals, banned claims, canonical tel:, https-only own-domain, img alt; port 3200). Same-job-after-build rule as budgets/smoke. Edit sanctioned by the QA-AUTO brief; rationale comment in the workflow. (S3 — informational)
- [x] [QA-MANUAL→QA-AUTO] /reviews double-wraps the section-owning ReviewWall: app/reviews/page.tsx:41–45 puts `<ReviewWall reviews={reviews}/>` inside an extra `<section class="section bg-paper-50"><div class="container-hm">` → rendered HTML nests two `.section` elements (verified in built reviews.html: `…container-hm"><section class="section bg-paper-50">`) = doubled vertical padding; the call also omits `trustpilotUrl`, so the placeholder note renders unlinked. Fix = render bare with `trustpilotUrl={globals.social.trustpilot}` (same one-line unwrap FE-3 did on /faqs). (S3 — MANUAL_QA F-01) **DONE by QA-AUTO 2026-06-12: rendered bare + trustpilotUrl wired; rebuilt and verified in reviews.html — 0 nested sections, Trustpilot link present in the wall note.**
- [x] [QA-MANUAL→QA-AUTO/DEVOPS] QuoteForm fixes for #21 (MIN_QUANTITY 25) + #42 (`data-quote-form`) verified present in source but landed AFTER the 17:57 verified build (QuoteForm.tsx mtime > BUILD_ID) — next CI rebuild carries them into the client bundle; then tick #21/#42. (S3 — MANUAL_QA F-02) **RESOLVED by QA-AUTO 2026-06-12: full rebuild(s) since (final gate pass) include the QuoteForm changes; #21/#42 ticks stand.**
- [x] [QA-MANUAL→DATA-ENG] content/globals.json `promo.href` is `"/get-custom-quote"` (no trailing slash) while every other internal href is trailing-slashed. next/link normalizes it in rendered HTML today (verified `href="/get-custom-quote/"`), but any non-Link consumer (email templates, JSON-LD, RSS) would 308-hop. Normalize the JSON value. (S3 — MANUAL_QA F-03) **DONE by QA-AUTO 2026-06-12 (one-char edit to DATA-ENG's file, logged): `"/get-custom-quote/"`; gates re-run green. NB lib/seo.ts FALLBACK_GLOBALS keeps the slash-less form — it only applies when globals.json is missing; SEO-2 may align it.**
- [ ] [QA-MANUAL→PM/client] ReviewWall's spec-prescribed note "Reviews collected via Trustpilot — verification in progress." overstates provenance while reviews.json is invented placeholders (`source:"placeholder"`). No code change requested — reinforces the existing launch blocker: import real Trustpilot reviews before cutover. (S3 — observation, MANUAL_QA F-05)
- [x] [SEO-VERIFY→QA-AUTO] 2 duplicate `<title>` pairs site-wide (CONTENT_GUIDELINES §11 wants titles unique): `/custom-display-boxes/` ↔ `/products/custom-display-boxes/` and `/custom-pizza-boxes/` ↔ `/products/custom-pizza-boxes/` — a category and its same-named product both resolve to "<Name> Wholesale | HM Custom Packaging" (CATEGORY_META and buildProductTitle produce identical strings). Meta descriptions/canonicals/H1-contexts differ, so no indexing risk — cosmetic SERP differentiation only. Suggested fix (SEO-2/BE-1): when product.name collides with a category title, drop to buildProductTitle Pattern B or add a differentiator (e.g. "<Name> | Custom Printed | HM Custom Packaging" ≤60). Corpus evidence: 212 pages scanned, descriptions 100% unique, only these 2 title collisions (docs/seo/SEO_VERIFICATION.md §3 obs.1). (S3) **DONE by QA-AUTO 2026-06-12 (Pattern-B option, edit to SEO-2's lib/seo.ts commented in-file): buildProductTitle now skips Pattern A when it would equal a CATEGORY_META title. Verified in rebuilt HTML — product pages render "Custom Pizza/Display Boxes | HM Custom Packaging" vs category "… Wholesale | …". Locked by 2 new tests incl. a dataset-wide no-collision scan (tests/seo.test.ts).**
- [x] [SEO-VERIFY→QA-AUTO/BE-2] 404 page (`_not-found.html`) inherits the HOME metadata from the root layout: meta description + canonical/og:url `https://www.hmcustompackaging.com/` render on the 404. KEYWORD_META_MAP §4 says 404 emits no meta description; a canonical on a 404 is also conceptually wrong (harmless in practice — real HTTP 404 status verified live + `robots: noindex` present, Google ignores canonicals on 404s). Fix when convenient: export metadata in `app/not-found.tsx` with title "Page Not Found | HM Custom Packaging", no description, no canonical (`alternates: { canonical: null }`), robots noindex (docs/seo/SEO_VERIFICATION.md §3 obs.2). (S3) **DONE by QA-AUTO 2026-06-12 (edit to BE-2's app/not-found.tsx, commented): description/alternates.canonical/openGraph/twitter nulled (clears layout inheritance) + explicit robots noindex; rebuilt _not-found.html verified — zero canonical/description/og:url tags.**
